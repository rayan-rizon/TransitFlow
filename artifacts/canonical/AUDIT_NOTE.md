# Canonical archive status

The July 8 bundle is the latest historical run, but it is not a passing
publication artifact. Its `gate_report.json` records `final_pass=false`.
Additionally, the primary detection score used simulator-truth ephemerides and
the reported corrected real-posterior distances were based on importance
resampling with collapsed effective sample size. Preserve this bundle for
provenance; use the current code and frozen runbook for all new claims.
